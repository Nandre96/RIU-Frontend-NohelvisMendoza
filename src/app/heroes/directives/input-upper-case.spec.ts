import { InputUpperCase } from './input-upper-case';

describe('InputUpperCase', () => {
  it('should create an instance', () => {
    const directive = new InputUpperCase();
    expect(directive).toBeTruthy();
  });
});
