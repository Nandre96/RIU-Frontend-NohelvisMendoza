import { ErrorMessageForm } from './error-message-form';

describe('ErrorMessageForm', () => {
  it('should create an instance', () => {
    const directive = new ErrorMessageForm();
    expect(directive).toBeTruthy();
  });
});
